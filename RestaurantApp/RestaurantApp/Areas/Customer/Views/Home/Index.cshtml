@model RestaurantApp.Models.ViewModels.IndexViewModel
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="text-center">
    <h1>Welcome to our Restaurant</h1>
</div>


@*<div class="backgroundWhite container">

    <ul id="menu-filters" class="menu-filter-list list-inline text-center">
        <li class="filter active btn btn-secondary ml-1 mr-1" data-filter=".menu-restaurant">Show All</li>
        @foreach (var item in Model.Category)
        {
            <li class="ml-1 mr-1" data-filter=".@item.Name.Replace(" ",string.Empty)">@item.Name</li>
        }
    </ul>
</div>*@




@foreach (var category in Model.Category)
{
    <div class="row" id="menu-wrapper">

        @if (Model.Category.Count() > 0)
        {

            <div class="col-12">

                <div class="row">
                    <h3 class="text-success"> @category.Name </h3>
                </div>

                @foreach (var menuitem in Model.MenuItem.Where(m => m.Category.Name.Equals(category.Name)).ToList())
                {
                    <div class="border border-info rounded col-12" style="margin-bottom:10px; margin-top:10px; padding:10px">

                        <div class="row">

                            <div class="col-md-3 col-sm-12">
                                <img src="@menuitem.Image" width="99%" style="border-radius:5px;border:1px solid #bbb9b9" />
                            </div>

                            <div class="col-md-9 col-sm-12">

                                <div class="row pr-3">
                                    <div class="col-8">
                                        <label class="text-primary" style="font-size:21px;color:maroon">@menuitem.Name</label>
                                    </div>
                                    <div class="col-4 text-right" style="color:maroon">
                                        <h4>$@menuitem.Price</h4>
                                    </div>
                                </div>


                                <div class="row col-12 text-justify d-none d-md-block">
                                    <p>@menuitem.Description</p>
                                </div>
                                <div class="col-md-3 col-sm-12 offset-md-9 text-center">
                                    <a asp-action="Details" class="btn btn-success form-control" asp-route-id="@menuitem.Id">Details</a>
                                </div>

                            </div>

                        </div>

                    </div>

                }
            </div>
            <div class="p-4"></div>
        }
    </div>
}




